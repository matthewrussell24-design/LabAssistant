# Full Python 3.12 macOS arm64 contributor environment.
# Regenerate with scripts/lock-dependencies; select a narrower lock in
# requirements/locks/ when only one application surface is needed.
-r requirements/locks/dev-py312-macos-arm64.txt
